## 13:00 — 修正 bootstrap.sh 預設 workspace 路徑

- **Agent**: Codex
- **Repo**: fleet-command
- **任務**: 修正 `./bootstrap/bootstrap.sh` 不帶 `--workspace` 執行時誤找 `fleet-command/fleet-command/workspace` 的路徑問題，讓 `./bootstrap/teardown.sh -Workspace <ws>` 兼容 PowerShell 風格參數，並校正 CardSense repos manifest refs 為遠端實際存在的 `master`；補上 shell dry-run 回歸測試。
- **結果**: 成功
- **PR**: 無
- **備註**: `uv` 不在目前 PATH，已改用 `python3` 跑同等 unittest；`git ls-remote` 確認四個 CardSense repo 目前沒有 `main`、有 `master`；dashboard 分類為 `workspace_rule_update`。
